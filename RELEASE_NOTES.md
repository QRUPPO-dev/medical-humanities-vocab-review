# v1.0.0

首个 GitHub 发布版本。

## 内容

- Windows 本地版压缩包
- macOS 本地版压缩包
- SHA-256 校验和

## 说明

- `main` 分支保存源码。
- `release` 分支保存下载包。
- macOS 包同时提供 `启动mac版.command` 和 `start-mac.command`。
- Windows 包同时提供 `启动Windows版.bat` 和 `start-windows.bat`。
